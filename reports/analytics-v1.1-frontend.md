# v1.1 前端交付与浏览器验证

2026-09-13，task #10，Stricter。前端基线 e900c96 加本报告所在提交；后端运行代码 a8189c0（e42561d 仅补文档）。测试实例 http://127.0.0.1:8770，独立合成库，不修改8765/8766或真实账本。

## 交付

完整浅色松绿分析页：双期总览、分期收支、盈余/累计切换、双向排行及其他展开、分类差额、性质拆分、日常结余率月线、自然日日均与三个月参照、已实现投资及累计、月历、12个月概览与版本保护分页。所有资源本地加载；保留原CSP。总额使用BigInt，均值通过整数分子/分母四舍五入至分；比例缺基数和负盈余分别处理。

图表为本地SVG；分期柱和折线点绑定各自系列，支持键盘Enter/Space、触屏模拟点击和数据入口。全局与局部方向/性质冲突返回明确空交集。分析与明细同版本才展示，409重取全部数据，自动刷新不追加旧页。

本次收尾修订：对比折线点不再误开本期；分类变化用水平正负条形；窄屏短序列适应容器、长序列内部滚动；窗口变化重算图表宽度；运行诊断显示视口及页面宽度。

## 已在真实Chrome中通过

使用CUA原生Chrome，桌面及DevTools Responsive 390×844（触屏模拟）。截图及AX证据在 `reports/ui-v11/`。该目录的 extend_fixture.py 可在原tests/make_fixture.py创建的disposable synthetic.db上生成相同80笔数据，当前月73笔。

| 场景 | 浏览器观测 |
|---|---|
| 桌面与390px | 首屏、日常负比例、投资、月历、明细可读；诊断明确视口390px、页面宽度390px；图表/表格仅内部滚动 |
| 分页 | 本期50/73 → 加载更多 → 73/73，按钮禁用 |
| 对比分类及返回 | 对比期明细标注8月1–12日；返回恢复9月本期明细 |
| 折线点系列 | 盈余对比点进入8月1日；投资非零对比点进入8月3日、投资限定、1/1笔，Enter再次激活仍保持正确日期 |
| 图例 | 隐藏本期收入，总收入3050元与50/73分母不变 |
| 粒度 | 日→月，主范围仍9月1–13日，对比8月1–13日；隐藏图例保留 |
| 排行 | 前5+其他3类，展开为全部；分类组生活→合成分类11，明细71→10笔 |
| 日历 | 无记录日期0笔；9月2日8笔→合成分类11一笔；未来日期禁用 |
| 全局交集 | 全局收入下点击9月2日支出日历，0/0笔，无支出泄漏；代码另加“与全局筛选无交集”说明 |
| 长期月份 | 点击8月概览，主范围8月1–31日，对比7月1–31日同步 |
| 自动新增 | 月粒度、收入图例隐藏、分类11选中时，外部合成新增id900=4321分：明细10→11，显示43.21元 |
| 自动修改 | 同行改9876分：页面显示98.76元、11笔 |
| 自动删除 | 删除合成id900：明细恢复10笔；分类、粒度及图例状态保留 |
| 金额特殊值 | 零基数显示无基数；净收支-6940.45、日常结余率-232.3%；均笔146.125元显示146.13；旧月投资无匹配显示零及空明细 |

## 验证边界

上述交互通过；尚未在实体手机或Safari/Firefox复演。完整短月/闰年/自选等日期组合由后端16项测试覆盖，本前端报告不将API验证冒充所有日期组合的浏览器复演。尚未逐一浏览器操作全年/全部长范围、任意自选对比、超大金额、服务故障/409竞争窗口；实现保留对应入口和保护，但这些属于集成补充验证项。自动刷新本次是新增/修改/删除3种操作各1次，不是20次性能基准；61.3ms为一次浏览器fetch至同步DOM完成诊断，非绘制时延承诺。

结论：已交付实现与上列浏览器证据，提交代码审核；未覆盖项目如上明确列出，最终集成验收与运行入口更新由老殷负责。

## 追加验收（2026-09-13 15:30–15:40，北京时间）

补齐原报告缺口：
- 年范围切换完整周期，实际显示2026-01-01至12-31，2025年完整对比；未来12月当前金额缺位。全部范围显示2024-02-29至2026-09-10、925天、80笔及“全部范围请自选对比日期”。原始AX见all-range-ax.txt。
- 自选对比在浏览器将结束日改为8月4日：本期9月1–13日13天，对比8月1–4日4天，明确“两期天数不同”；第5期起对比金额缺位，不伪造零。见custom-compare-ax.txt。
- 合成竞争复现：仅QA代理8771在analytics成功后、dashboard到达前写入id3000，转发到8770真实后端，由后端版本校验返回409。Chrome自动完整重取，最终50/94笔、诊断409重同步1次；版本d145ab99be0fec9f，页面已同步。见race-409-ax.txt、race-evidence.json、race-409.log。race_proxy.py为QA脚本，复制到qa-runtime运行（该目录包含synthetic.db及arm-race标记）；不进入应用运行路径。
- 20次外部提交计时：浏览器持续前台，间隔3秒的独立SQLite提交，记录commit epoch及后端version；页面在同步DOM完成后记录epoch/version；两者按version一一配对20/20，最小99ms、最大2067ms、p95 2043ms，全部≤5000ms。不是API耗时，亦非显示器绘制时刻；是提交完成到浏览器同步DOM更新的时延。见commit-times.json、refresh-20-ax.txt、refresh-paired.json及timed_commits.py。
- 第一轮用于检查诊断输出后重跑；上述20条属于第二轮完整独立记录。合成测试id1000–1019仅在隔离库，版本不同且逐次更新，未碰真实账本。

本轮要求的全年/全部、自选比较、409竞争重同步及20次前台5秒验收均通过。实体手机、其他浏览器及穷举所有日期组合不在本次浏览器矩阵；任意超大金额边界仍未单独浏览器实测。原报告“20次未覆盖”及“409未覆盖”由本节替代。

## 自选范围控件修复（2026-09-13 21:33，北京时间）

针对最终审核发现的自选日期口径问题，前端在 `kind=custom` 时隐藏并禁用“本期进度”控件，并固定向 API 发送 `period_mode=full`；切回月等自然周期后恢复控件以及原先选择的“截至今天/完整周期”状态。

真实 Chrome 在 8770 隔离合成实例复演通过：从月/截至今天切换自选，AX 树中进度控件消失；将结束日设为 2026-09-30 后页面完整显示 2026-09-01 至 2026-09-30（30 天），未截断到 9 月 13 日；切回月后进度控件恢复为“截至今天”，主范围恢复 9 月 1–13 日。该实例运行的旧后端仍会按 `period_mode=full` 保留显式范围，证明前端请求防线和控件语义一致；最终后端 `00f8744` 另修复 custom 对 elapsed 的容错。
